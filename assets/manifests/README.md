# 资产 manifest

为 Piper X 和 Isaac Sim 外部资产分别记录：上游项目、精确 URL、commit 或发布版本、许可证、文件校验值、获取日期、转换步骤、坐标和单位约定，以及仓库内期望的相对挂载位置。

资产版本尚未确认时使用 TBD，不提交未经确认的下载脚本或第三方二进制文件。

当前 [`piper_x_agx_history_candidate_v0.1.yaml`](piper_x_agx_history_candidate_v0.1.yaml) 只记录用户允许参考的历史 `agx_datacollect_ws` Piper X 文件、commit、校验值和 joint contract。它是 Candidate，不代表已选为最终 Isaac Sim 资产；生成 USD 后还需补充 6.1.0 import manifest 与场景 smoke test。
