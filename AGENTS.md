# FragileGrabVLA 开发规则

## 开始改动前

开始任何代码、配置或文档改动前，必须先阅读：

- `README.md`
- `docs/architecture.md`
- `docs/status.md`

以 GitHub 仓库中的代码和文档作为项目的长期事实来源。聊天记录可以辅助讨论，但未写入仓库的内容不应被视为已确认决策。

## 当前范围

- 课题是易碎物品抓取。
- 项目长期定位是围绕 Piper X 与 π0.5，维护真实数据采集、数据处理、仿真验证、策略执行、易碎性研究和统一评测的研究工作区。
- 当前仅计划使用 π0.5；近期工程目标仍是验证 Piper X 数据链路，并在 Isaac Sim 中打通简单抓取的最小闭环并将建立一套fragilegrab的评价体系。
- 保留 π0.5 原生 action chunk 范式，不得未经确认改造成单步动作或通用模型输出。
- 不得擅自引入 OpenVLA、其他 VLA，或面向多模型的 backend、adapter、policy registry 等抽象。
- 易碎物品安全机制是 **Open Research Question / TBD**。不得实现、命名或假定项目中已经存在 Safety Layer。

## 状态与证据

- `Confirmed` 表示已经由项目成员明确决定，但不代表运行验证完成。
- `Verified` 只用于已有代码运行、仿真或真实机器人实验依据的能力；仅有代码或说明文档不能视为 Verified。
- `In Progress` 表示正在实现或验证；未知内容统一标记为 `TBD`，候选方案明确标记为 `Candidate`。
- 真实数据采集、仿真、推理和评测结果应能够追踪配置、软件版本、设备或资产版本及实验产物。

## 官方资料与实现依据

- 涉及 OpenPI、π0.5、Isaac Sim 或 Piper 的代码、配置和运行方式时，必须先确认实际使用的版本或 Git commit，并阅读该版本的官方代码、文档和示例；不得凭经验猜测接口或自行补全未确认行为。
- OpenPI/π0.5 当前参考基线是用户提供的 OpenPI 归档，对应 commit `15a9616a00943ada6c20a0f158e3adb39df2ccac`。实现前应直接核对归档中的相关模块、配置与示例；若实际运行环境的版本不同，必须按实际版本重新核对。
- 输入字段、数据类型、张量形状、图像预处理、state/action 语义、归一化、action chunk 以及推理服务协议，必须与所用 OpenPI 版本的官方实现保持一致。官方资料未确定的项目映射继续标记为 TBD。
- Isaac Sim 实现必须依据队友实际安装版本的官方文档和示例；Piper 资产与控制接口必须核对官方来源、具体型号、版本、许可证、关节定义和单位。
- 优先使用官方公开接口和推荐工作流，只编写完成 Piper X 仿真任务所需的最小项目代码。社区资料只能辅助定位兼容性或运行问题，不能替代官方接口定义。
- 如确需偏离官方示例或推荐方法，实施前必须说明原因、影响和替代方案并交由用户审核；验证时应记录所依据的版本、来源以及与官方示例的对照结果。
- 高层文档只记录稳定目标、架构、职责和接口语义，不写入机器专属路径、临时命令、魔法参数或易过时的实现片段。具体参数进入配置，版本相关步骤进入实现文档或运行手册，核心逻辑不得堆积在一次性脚本中。

## 配置与模块边界

- 本机路径、设备编号、CAN 接口、相机序列号、IP、端口、频率、episode 参数、checkpoint 路径、仿真参数和评测阈值不得作为核心逻辑中的项目常量；应通过配置、环境变量或不提交的本地配置提供。
- 真机采集、Isaac Sim、π0.5 和 evaluation 可以共享明确的数据语义与记录格式，但应保留各自的设备或运行时实现边界。
- 数据采集是核心研究基础设施。修改前必须先理解其相机、机器人状态、action、时间同步、episode、序列化和依赖关系，并优先保留已经验证的运行能力。
- 只有在第二个真实实现出现并证明存在公共行为后，才提取通用抽象；不得为了目录美观创建空模块或无实际使用者的接口。

## 改动与文档同步

- 重大架构变更须先明确提出，并在实施时同步更新 `docs/architecture.md`。
- 项目阶段、已确认事实、研究问题或下一里程碑变化时，同步更新 `docs/status.md`。
- 未确认的接口、参数和技术方案必须标记为 TBD，不得写成项目事实。

## Git 与产物

- 代码、配置和项目事实进入 GitHub。
- 数据集、视频、模型权重、checkpoint、运行日志及 `runs/` 等大文件或生成产物不得提交，应通过 `.gitignore` 排除并保存在本地或外部存储。
- 不提交密钥、令牌、机器专属路径或其他敏感信息。
